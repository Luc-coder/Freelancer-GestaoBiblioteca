<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../css/main.css">
    <link rel="stylesheet" href="../css/editarLivro.css">
</head>
<body>


    <header>
        <div class="cabecalho">
            <div class="arrow">
                <a href="/atualizaLivro"><img src="../images/seta-esquerda.png" /></a>
            </div>
            <div class="title">
                <h1>EDITAR LIVRO</h1>
            </div>
            <div class="logout">
                <a href="/"><img src="../images/sair.png"></a>
            </div>
        </div>
    </header>

    <form class="container" action="/atualizarLivro" method="post">
        <!--nao precisa estilizar o input type hidden-->

        
        <div class="card color">
            <input type="hidden" name="id" value="<%= livro.id %>">
            <label for="">Nome</label>
            <input type="text" name="name" value="<%= livro.name %>">
            <br>
            <label for="">Autor</label>
            <input type="text" name="author" value="<%= livro.author %>">
            <br>
            <label for="">Gênero</label>
            <!---->
            <!-- <input type="text" name="genero" value="<%= livro.genero %>"> -->
            <select name="genero" id="">
                <option value="acao" <%= livro.genero === 'acao' ? 'selected' : '' %>>Ação</option>
                <option value="aventura" <%= livro.genero === 'aventura' ? 'selected' : '' %>>Aventura</option>
                <option value="ficcao" <%= livro.genero === 'ficcao' ? 'selected' : '' %>>Ficção</option>
                <option value="suspense" <%= livro.genero === 'suspense' ? 'selected' : '' %>>Suspense</option>
                <option value="terror" <%= livro.genero === 'terror' ? 'selected' : '' %>>Terror</option>
                <option value="comedia" <%= livro.genero === 'comedia' ? 'selected' : '' %>>Comédia</option>
                <option value="quadrinho" <%= livro.genero === 'quadrinho' ? 'selected' : '' %>>Quadrinho</option>
                <option value="biografia" <%= livro.genero === 'biografia' ? 'selected' : '' %>>Biografia</option>
            </select>
            <br>
            <label for="">Data de Publicação</label>
            <input type="date" name="data_publicacao" value="<%= formatDate(livro.data_publicacao) %>">
            <br>
            <label for="">Editora</label>
            <input type="text" name="editora" value="<%= livro.editora %>">
            <br>
            <br>
            <button class="buttom" type="submit">Atualizar Livro</button>
        </div>
    </form>

    <% function formatDate(date) {
        return date ? new Date(date).toISOString().split('T')[0] : '';
    } %>
    <footer>
        <p>Todos os direitos reservados - &copy; 2023</p>
    </footer>
</body>
</html>
